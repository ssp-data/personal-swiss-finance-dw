.DEFAULT_GOAL := serve

serve:
	( \
       source ~/.venvs/banking-bekb//bin/activate; \
       dagster dev; \
    )

install:
	( \
       source ~/.venvs/banking-bekb//bin/activate; \
       pip install -e ".[dev]"; \
    )
